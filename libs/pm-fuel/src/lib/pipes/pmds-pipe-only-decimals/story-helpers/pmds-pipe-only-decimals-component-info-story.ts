export const componentInfo = `
Import: **PmdsPipeOnlyDecimals** from **../../../../index**

~~~~
import {
	PmdsPipeOnlyDecimals
} from '../../../../index';
~~~~

Add to import array the **PmdsPipeOnlyDecimals** in your component

~~~~
imports: [
	PmdsPipeOnlyDecimals
]
~~~~

Use this pipe to get only the decimal part of a number, use it for get a diferent style in decimal part:`
