export const componentInfo = `
Import: **PmdsPipeTransactionReference** from **../../../../index**

~~~~
import {
	PmdsPipeTransactionReference
} from '../../../../index';
~~~~

Add to import array the **PmdsPipeTransactionReference** in your component

~~~~
imports: [
	PmdsPipeTransactionReference
]
~~~~

Use this pipe to change for '-'(or other text) the 'NOTPROVIDED', 'NONREF' literals:`
