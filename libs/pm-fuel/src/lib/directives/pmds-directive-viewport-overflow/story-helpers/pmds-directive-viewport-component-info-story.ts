export const componentInfo = `
Import: **PmdsDirectiveViewportOverflow** from **../../../../index**

~~~~
import {
	PmdsDirectiveViewportOverflow
} from '../../../../index';
~~~~

Add to import array the **PmdsDirectiveViewportOverflow** in your component

~~~~
imports: [
	PmdsDirectiveViewportOverflow
]
~~~~

Use this directive to translate element if overflow bottom of viewport, is necessary to add the height element and total to translate in Y axys for calculate the transition:

`
