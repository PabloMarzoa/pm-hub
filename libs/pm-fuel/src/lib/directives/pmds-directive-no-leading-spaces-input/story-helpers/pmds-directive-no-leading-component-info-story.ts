export const componentInfo = `

Import: **PmdsDirectiveNoLeadingSpacesInput** from **../../../../index**

~~~~
import {
	PmdsDirectiveNoLeadingSpacesInput
} from '../../../../index';
~~~~

Add to import array the **PmdsDirectiveNoLeadingSpacesInput** in your component

~~~~
imports: [
	PmdsDirectiveNoLeadingSpacesInput
]
~~~~

Use this directive to disallow entering spaces at the beginning of the value:

`;
