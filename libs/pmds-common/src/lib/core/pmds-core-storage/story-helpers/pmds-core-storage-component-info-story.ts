export const componentInfo = `
This service is used for use local storage

Import **PmdsCoreStorageService** service from @pmhub/pmds-common

~~~
import { PmdsCoreStorageService } from '../pmds-core-storage.service';
~~~

Inject **PmdsCoreStorageService** service

~~~

private storageSrv = inject(PmdsCoreStorageService);
~~~

For use it:`
