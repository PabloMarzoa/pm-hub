export const componentInfo = `
This service is used to cache information

Import **PmdsCoreCacheService** service from @pmhub/pmds-common

~~~
import { PmdsCoreCacheService } from '../pmds-core-cache.service';
~~~

Inject **PmdsCoreCacheService** in your component

~~~

private cacheSrv = inject(PmdsCoreCacheService);
~~~

For use it create a identifier and cache data or recover data cached
`
