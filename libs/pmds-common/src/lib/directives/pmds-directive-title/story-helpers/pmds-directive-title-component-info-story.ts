export const componentInfo = `
Import: **PmdsDirectiveTitle** from **@pmhub/pmds-common**

~~~~
import {
	PmdsDirectiveTitle
} from '@pmhub/pmds-common';
~~~~

Add to import array the **PmdsDirectiveTitle** in your component

~~~~
imports: [
	PmdsDirectiveTitle
]
~~~~

Use this directive to add title attribute and add elipsis to text when the container is less that the content, if you hover a fews seconds over text it will show all the text:

`
