export const componentInfo = `

Import: **PmdsDirectiveTooltip** from **@pmhub/pmds-common**

~~~~
import {
	PmdsDirectiveTooltip
} from '@pmhub/pmds-common';
~~~~

Add to import array the **PmdsDirectiveTooltip** in your component

~~~~
imports: [
	PmdsDirectiveTooltip
]
~~~~

Use this directive to add a tooltip in html elemnt:

`
