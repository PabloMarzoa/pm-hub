export const componentInfo = `

Import: **PmdsDirectiveMaskAmountInput** from **@pmhub/pmds-common**

~~~~
import {
	PmdsDirectiveMaskAmountInput
} from '@pmhub/pmds-common';
~~~~

Add to import array the **PmdsDirectiveMaskAmountInput** in your component

~~~~
imports: [
	PmdsDirectiveMaskAmountInput
]
~~~~

Use this directive with inputs pmdsMAIDecimalCharacter and pmdsMAIMaxIntLength to force a mask value in numbers input:

`;
