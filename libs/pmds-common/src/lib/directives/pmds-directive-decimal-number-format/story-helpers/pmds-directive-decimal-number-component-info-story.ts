export const componentInfo = `

Import: **PmdsDirectiveDecimalNumberFormat** from **@pmhub/pmds-common**

~~~~
import {
	PmdsDirectiveDecimalNumberFormat
} from '@pmhub/pmds-common';
~~~~

Add to import array the **PmdsDirectiveDecimalNumberFormat** in your component

~~~~
imports: [
	PmdsDirectiveDecimalNumberFormat
]
~~~~

Use this directive to parse decimal chart to '.', try write 123,45:

`;
