export const componentInfo = `
Import: **PmdsPipeTypeOf** from **@pmhub/pmds-common**

~~~~
import {
	PmdsPipeTypeOf
} from '@pmhub/pmds-common';
~~~~

Add to import array the **PmdsPipeTypeOf** in your component

~~~~
imports: [
	PmdsPipeTypeOf
]
~~~~

Use this pipe to check the type from value:`
