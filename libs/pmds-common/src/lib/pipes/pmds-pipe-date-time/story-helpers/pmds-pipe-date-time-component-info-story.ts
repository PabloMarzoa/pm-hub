export const componentInfo = `

Import: **PmdsPipeDateTime** from **@pmhub/pmds-common**

~~~~
import {
	PmdsPipeDateTime
} from '@pmhub/pmds-common';
~~~~

Add to import array the **PmdsPipeDateTime** in your component

~~~~
imports: [
	PmdsPipeDateTime
]
~~~~

Use this pipe to format date with timezone, <a href="https://en.wikipedia.org/wiki/List_of_tz_database_time_zones#List" target="_blank">timezone id list</a>:`
