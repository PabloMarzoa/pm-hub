export const componentInfo = `
Import: **PmdsPipeSalesforceNumberFormat** from **@pmhub/pmds-common**

~~~~
import {
	PmdsPipeSalesforceNumberFormat
} from '@pmhub/pmds-common';
~~~~

Add to import array the **PmdsPipeSalesforceNumberFormat** in your component

~~~~
imports: [
	PmdsPipeSalesforceNumberFormat
]
~~~~

Use this pipe to format number with salesforce mask:`
