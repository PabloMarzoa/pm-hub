export function EndPage() {
  return <section className="flex flex-col justify-center items-center h-screen w-full snap-start snap-always">
    <h1 className="pmds-fade !text-[8rem] !text-slate-600">FUEL</h1>
    <h3 className="pmds-fade !text-[2rem] !text-slate-600">Combustible to super charge your apps</h3>
  </section>
}
